# Example suffix definitions for `scraper_images.py`.
# Copy this file to `custom_suffixes.py` and replace the values.

# Map product titles to a list of possible suffixes used when generating
# image alt text. Titles are slugified automatically, so the keys can be
# written normally.
custom_suffixes = {
    "My product title": ["custom suffix"],
}

